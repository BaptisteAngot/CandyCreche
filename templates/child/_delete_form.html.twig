<form method="post" action="{{ path('child_delete', {'id': child.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer cet enfant ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ child.id) }}">
    <div class="save">
    <button class="btn">Supprimer</button>
    </div>
</form>
