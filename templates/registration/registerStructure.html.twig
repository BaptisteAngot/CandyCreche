{% extends 'base.html.twig' %}

{% block body %}

    <h1>Register</h1>

    {{ form_start(registrationForm) }}

    {{ form_row(registrationForm.structureMail) }}
    {{ form_row(registrationForm.structurePassword) }}


    <input type="submit" value="Register">
    {{ form_end(registrationForm) }}


{% endblock body %}