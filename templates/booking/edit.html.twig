{% extends 'base.html.twig' %}

{% block title %}Edit Booking{% endblock %}

{% block body %}
    <div class="imagedefond">
    <h1>Modification</h1>
        <div class="centrer">
    {{ include('booking/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn-ff" href="{{ path('booking_index') }}">Retour à la liste</a>

    {{ include('booking/_delete_form.html.twig') }}
        </div>
    <div class="clear"></div>
    </div>
{% endblock %}
