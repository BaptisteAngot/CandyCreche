<header>
        <nav class="navbar">
            <div class="container">
                <div class="logo">
                    <a href="{{ path('accueil') }}">Candy Crèche</a>
                </div>
                <div id="mainListDiv" class="main_list">
                    <ul class="navlinks">

                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            {% if is_granted('ROLE_PARENT') %}
                                <li><a href="{{ path('profil') }}">Profil</a></li>
                                <li><a href="{{ path('liste') }}">liste Structure</a></li>
                            {% elseif is_granted('ROLE_STRUCTURE') %}
                                <li><a href="{{ path('profilstructure') }}">Profil</a></li>
                            {% endif %}

                            <li><a href="{{ path('structurelogout') }}">Logout</a></li>
                        {% else %}
                            <li><a href="#">Qui sommes-nous ?</a></li>
                            <li><a href="#parallax-two-parents">Espace Parents</a></li>
                            <li><a href="#parallax-two-entreprises">Espace Entreprises</a></li>
                        {% endif %}

                    </ul>
                </div>
                <span class="navTrigger">
                <i></i>
                <i></i>
                <i></i>
            </span>
            </div>
        </nav>
</header>